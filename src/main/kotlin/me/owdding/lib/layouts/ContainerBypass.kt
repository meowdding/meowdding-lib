package me.owdding.lib.layouts

/**
 * Used to mark widgets that will receive mouse scroll events in container screens,
 * by default they don't get passed on to the super screen.
 */
interface ContainerBypass
